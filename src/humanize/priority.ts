import { ParsedType } from "./types";

export const LOW_RELEVANCE_TYPES: ParsedType[] = [
    'COMPUTE_BUDGET'
]

export const getRevelance = (type: ParsedType) => {
    // typeof type == 'COMPUTE_BUDGET'
}

